* Copy acq4xx.py into your MDSPLUS_PYDEVICE_PATH

If you want to use MGT482 SFP streaming:

* Copy ./mgt to /mgt
* Create user mdsplus with PASSWD less sudo acces to /mgt/mgt-arm, /mgt/mgt-deinit, and /mgt/mgt-stream
* Get driver from https://github.com/D-TACQ/AFHBA404
* Compile kernel module AFHBA404/afhba.ko and place it in /mgt
* Compile AFHBA404/STREAM/rtm-t-stream-disk and place it in /mgt
* Run your action server as user mdsplus


NOTE: You can change the user name
